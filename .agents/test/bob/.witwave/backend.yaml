backend:
  agents:
    - id: claude
      url: http://localhost:8001
      model: claude-opus-4-7

    # OpenAI and Gemini are intentionally omitted from the active Bob smoke
    # deployment. Keep their backend config directories as parked fixtures,
    # and add them back through `ww agent backend add` or a fresh multi-backend
    # `ww agent create` only when credentials and budget are available.

  routing:
    default:
      agent: claude
      model: claude-opus-4-7
    a2a:
      agent: claude
      model: claude-opus-4-7
    heartbeat:
      agent: claude
      model: claude-opus-4-7
    job:
      agent: claude
      model: claude-opus-4-7
    task:
      agent: claude
      model: claude-opus-4-7
    trigger:
      agent: claude
      model: claude-opus-4-7
    continuation:
      agent: claude
      model: claude-opus-4-7
